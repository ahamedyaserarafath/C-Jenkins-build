#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World!");
   printf("checking the git fetch");
   printf("checking the ci build");
   printf("checking the ci build error");
   return 0;
}
